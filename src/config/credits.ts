// クレジットに必要で、既存の置き場が無い正典の文言（値のみ）。
// config は値の定義のみを持つ層であり、上位のサブシステムを取り込まない。

/**
 * AIが生成した素材を使っていない旨。文言は正典に基づく。
 * docs/concept.md（プログラミングの出力として表示される音楽・絵画・文章にAI生成物を利用しない）と、
 * docs/idea/concept-final.md（表示する絵・音楽・文章にAI生成物を使わない）に対応する。
 * 末尾の一文は、規約のAIの境界が「素材をAIが生成しているか」である事実に沿い、
 * 視覚表現がコードによる動的描画である旨を述べるために加える。
 */
export const AI_PROVENANCE_STATEMENT =
  "本作品が表示する絵・音楽・文章に、AIが生成した素材は使用していません。視覚表現はすべてコードによる動的描画です。";
