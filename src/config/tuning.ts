// 楽曲非依存のチューニング定数（調整値）を一元管理するモジュール。
// ここに置くのは「楽曲が変わっても同じ値であり、複数のサブシステムが横断的に参照する調整値」だけである。
// 値は作品仕様・調査の正典と照合済みである。出典は各定数のコメントにファイル名と節で記す。
// このモジュールは値の定義のみを持ち、判定式・減衰式などの論理は持たない（責務の出典 src/config/README.md、docs/decisions/architecture.md §4）。
//
// 状態の記号:
//   ★暫定 = docs/research/07-feasibility-and-parameters.md §2 と docs/decisions/app-overall-decisions.md §5 に基づき、
//           見積もりを初期値とし、実装後のプレイ検証で調整する値。
//   ☆確定 = 標準仕様・人間工学・作品仕様で固定される値。
//
// ここに置かない値（理由は docs/decisions/architecture.md §3.6・§5 の「曲非依存のエンジンと曲ごとのプロファイルの分離」）:
//   - 楽曲固有の絶対値（1拍343ミリ秒・毎分175拍、フルに可能なタップ数434、タップ総数上限の絶対値260、
//     見せ場6箇所、セクション密度、同時押し2点から3点、量子化の絶対値、満タンゲージの絶対タップ数など）
//     は曲プロファイル（Issue #34・#46）と譜面（Issue #38・#39・#44）に置く。
//   - 単一の所有モジュールが定まる値（得点合成の基準値は scoring の #55・#56、ランクの段階は scoring の #55 と types、
//     ウォームアップのカウントダウンは screens の #2、性能閾値は rendering の #18）は、その所有モジュールが定義する。
//   - 原典に単一の確定値の記載がない値（コンボの総得点に占める上限割合など）は推測で埋めず、所有Issueが確定する。

// --- 判定窓 ---
// 楽曲非依存の人間のタイミング許容。判定窓は時間（ミリ秒）で定め、各ノーツ点でカメラ軌跡上の速さを掛けて
// 距離の窓へ翻訳する（翻訳と減衰の論理は Issue #48）。
// 出典: docs/research/04-ux-and-chart-design.md §1「判定窓」、docs/research/07-feasibility-and-parameters.md §2.4。
// 消費Issue: #48（タップ判定エンジン）、#55（スコアリング合成）。

/** 満点とする判定の許容幅。JUSTの中心の前後40ミリ秒以内を満点とする。★暫定。 */
export const JUDGE_PERFECT_WINDOW_MS = 40;

/** 線形減衰の外端。前後40ミリ秒から90ミリ秒で得点を線形に減らし、90ミリ秒を超えると床の得点（発音のみ、光点は最小）とする。★暫定。 */
export const JUDGE_DECAY_OUTER_WINDOW_MS = 90;

/** タイミング精度の点推定に用いる参考窓（1拍の約17パーセント）。判定窓の調整範囲は前後40ミリ秒から90ミリ秒である。★暫定。 */
export const JUDGE_POINT_ESTIMATE_WINDOW_MS = 60;

// --- 音程スロット ---
// 画面の縦方向（Y軸）を分割し、その瞬間の和音の構成音を割り当てる数。コードトーン格子はエンジン中核であり曲非依存。
// 安全付加音（長調は9度と6度、短調は♭7度と11度）の音高集合の表現は和音名パーサー（#35）と JUST 生成（#36）が定義する。
// 出典: docs/research/07-feasibility-and-parameters.md §2.3、docs/idea/concept-final.md §4。
// 消費Issue: #36（JUST音程7スロット自動生成）、#48、#49（軌跡上距離と時間の翻訳）。

/** 音程スロット数の既定値。3和音6音に根音の重複か安全付加音を1つ加えた数。★暫定。 */
export const PITCH_SLOT_COUNT_DEFAULT = 7;

/** 音程スロット数の調整範囲の下限。★暫定。 */
export const PITCH_SLOT_COUNT_MIN = 5;

/** 音程スロット数の調整範囲の上限。★暫定。 */
export const PITCH_SLOT_COUNT_MAX = 9;

// --- 一回性（タップ総数上限の比率） ---
// 楽曲非依存なのは「フルに可能なタップ数に対する上限の割合」だけである。
// 楽曲固有の絶対値（フル434、上限260、範囲170から350）は TAKEOVER の曲プロファイルと譜面（#44・#46）に置く。
// 出典: docs/decisions/app-overall-decisions.md §3.7、docs/research/07-feasibility-and-parameters.md §2.2。
// 消費Issue: #44（タップ総数上限算出）、#55。

/** タップ総数上限の比率の既定値。フルに可能なタップ数の6割を目安とする。★暫定。 */
export const TAP_LIMIT_RATIO_DEFAULT = 0.6;

/** タップ総数上限の比率の調整範囲の下限（フルの4割）。下げすぎると失敗のない床と矛盾する。★暫定。 */
export const TAP_LIMIT_RATIO_MIN = 0.4;

/** タップ総数上限の比率の調整範囲の上限（フルの8割）。上げると取り切れて一回性が薄れる。★暫定。 */
export const TAP_LIMIT_RATIO_MAX = 0.8;

// --- ゲージ蓄積 ---
// 楽曲非依存なのは蓄積の倍率だけである。満タンの絶対タップ数（約50）と満タンあたりの投下回数（5）は
// TAKEOVER の上限260と見せ場6箇所から導出する曲依存の値であり、ゲージ機構（#54）と曲プロファイルに置く。
// 出典: docs/research/07-feasibility-and-parameters.md §2.5、docs/decisions/app-overall-decisions.md §3.6。
// 消費Issue: #54（ゲージ・投下システム）、#55。

/** ゲージ蓄積の両JUST倍率。タイミングと音程の片方成功で基本量、両方がJUSTのときその2倍を蓄積する。★暫定。 */
export const GAUGE_BOTH_JUST_MULTIPLIER = 2;

// --- 入力の人間工学 ---
// 片手の親指の精度を保つための、タップ対象の最小の大きさ。音程スロットの各ゾーン幅の下限を与えるため
// スロット数と横断的に連動する。画素または長さのいずれかで48画素または11ミリメートル以上とする。
// 出典: docs/research/04-ux-and-chart-design.md §4「横持ち両手の操作性」。
// 消費Issue: #47（3空間分離入力アーキテクチャ）、#49。

/** タップ対象の最小の大きさ（画素）。☆確定。 */
export const MIN_TOUCH_TARGET_PX = 48;

/** タップ対象の最小の大きさ（ミリメートル）。☆確定。 */
export const MIN_TOUCH_TARGET_MM = 11;

// --- 表示粒度の切替（#29 表示粒度切替コントローラ） ---
// 文字・単語・フレーズ・画面全体の4段の表示粒度を、歌詞の発声属性から選ぶための知覚境界・比率・拍数。
// すべて楽曲非依存である（曲名・テンポ・拍間隔・特定曲の文字数を含まない）。
// 出典: docs/idea/concept-final.md §11、docs/research/01-kinetic-typography.md §9、docs/analysis/song-insights.md。
// 消費Issue: #29（表示粒度切替コントローラ）。

/**
 * 短音とみなす文字の継続時間の上限（ミリ秒）。song-insights.md が短音率を「150ミリ秒以下」で
 * 定義して3曲を横断比較しており、この定義に揃えて境界の取り違えを防ぐ。1拍343ミリ秒のおよそ
 * 半分で瞬間的な短音と読める境界である。★暫定。
 */
export const GRANULARITY_SHORT_CHAR_MAX_MS = 150;

/**
 * 長音とみなす文字の継続時間の下限（ミリ秒）。song-insights.md が長音率を「500ミリ秒以上」で
 * 定義しているためこれに揃える。1拍343ミリ秒を超えて伸びる音を伸ばし音と読める境界である。★暫定。
 */
export const GRANULARITY_LONG_CHAR_MIN_MS = 500;

/**
 * フレーズを短音優勢とみなす、短音の文字数のフレーズ全文字数に対する比率の下限。
 * 半数以上が短音なら、そのフレーズの主たる発声性格を短音と判断できる。★暫定。
 */
export const GRANULARITY_SHORT_DOMINANT_RATIO = 0.5;

/**
 * フレーズを長音主体とみなす、長音の文字数のフレーズ全文字数に対する比率の下限。
 * 長音を1文字でも含むだけで長音主体とすると、短音が多数のフレーズ（実データの「NaNaNaNa」は
 * 長音1文字・短音75パーセント）を誤分類するため、多数決の比率で判断する。★暫定。
 */
export const GRANULARITY_LONG_TONE_DOMINANT_RATIO = 0.5;

/**
 * フレーズの文字密度（1拍あたり文字数）を高密度とみなす下限。1拍あたり1文字を超えると毎拍に
 * 1文字を割り当てられず、研究資料の「2拍に1回へ間引き」が必要になる。★暫定。
 */
export const GRANULARITY_DENSE_CHARS_PER_BEAT = 1.0;

/**
 * 声量を大きいとみなす、曲の最大声量に対する比率の下限。声量の絶対値は曲ごとに異なるため、
 * 曲自身の最大値に対する比率で判断する。★暫定。
 */
export const GRANULARITY_HIGH_LOUDNESS_RATIO = 0.6;

/**
 * 一画面に一括表示して読める文字数の上限。最小表示寸法を満たす文字を横一列に並べて画面に
 * 収まる文字数の見積もりであり、これを超えるフレーズは分割または流し込みが要る。曲でなく
 * 画面と最小表示寸法に依存する曲非依存の値であり、最小表示寸法を確定する #31 の完了後に再調整する。★暫定。
 */
export const GRANULARITY_READABLE_CHARS = 14;

/**
 * フレーズの連発（近接反復）とみなす、同一フレーズ文字列の再出現を許す時間の窓（ミリ秒）。
 * 実データで近接反復は前後3フレーズ以内（おおむね数秒以内）であり、遠い再登場（前後32フレーズ以上）
 * は連発でない。窓を時間で定めるため曲のテンポに依存しない。★暫定。
 */
export const GRANULARITY_NEAR_REPEAT_WINDOW_MS = 8000;

/**
 * フレーズ間の無音を画面全体の切れ目（暗転）とみなす最小の拍数。1拍程度の短い切れ目で暗転すると
 * 慌ただしく、2拍以上の無音を曲の切れ目とみなすのが自然である。拍数で定めるため曲のテンポに依存しない。★暫定。
 */
export const GRANULARITY_FULLSCREEN_MIN_GAP_BEATS = 2;

// --- 拍同期ポストエフェクト（#17 色収差バーストの減衰） ---
// 強拍（小節頭）で出した色収差を時間とともに消すための減衰時定数。楽曲非依存の時間値であり、本編結線（#59）の
// app 統括と描画診断（#17）の双方が、拍バースト包絡（src/utils/beatBurstEnvelope.ts）の入力として参照する。
// 出典: docs/research/02-non-text-expression.md §2、docs/analysis/takeover.songmap.json。
// 消費Issue: #17（診断ページの駆動）、#59（本編プレイへの結線）。

/**
 * 色収差バーストの減衰時定数（ミリ秒）。強度は exp(マイナス経過時間 ÷ この時定数)で減る。採用理由を先に述べる。
 * 強拍の間隔は小節長およそ1029から1372ミリ秒（3から4拍×1拍343から376ミリ秒、takeover.songmap.json 実測）であり、
 * この時定数の3倍の360ミリ秒で強度が5パーセント以下まで減衰すると、次の強拍より十分早く消えてバーストが
 * 重ならない。短すぎると瞬きが知覚されず、長すぎると色ずれが残留して不快になるため、80から150ミリ秒で
 * 実機調整する。★暫定。
 */
export const POST_CHROMA_DECAY_TAU_MS = 120;

// --- 演出合成エンジン（#131 複数演出の重ね適用） ---
// 読ませる役の透明度下限と、1フレームの性能上限。いずれも楽曲非依存である。
// 性能上限の絶対値は正典に実機計測が無く、毎秒60フレームの全体検証は #97 に置かれるため、ここでは機構を
// 成立させる保守的な初期値とする。費用合算（src/typography/kineticText/effectBudget.ts）は上限を引数 caps で
// 受け取る純粋関数であり、駆動側（#33・#59）がこれらの値を caps へ写す。同時表示文字数の上限は実行時関数
// computeMaxConcurrent が決めるため定数では持たず、駆動側が LayerLimits.single を caps へ渡す。
// 出典: Issue #131 本文「性能上限と縮退」、docs/decisions/architecture.md §3.8。
// 消費Issue: #131（合成器の透明度下限）、#33・#59（caps への注入）、#97（実機計測での確定）。

/**
 * 読ませる役の透明度の下限。採用理由を先に述べる。複数演出の透明度の積が読ませる役を過度に薄くして
 * 歌詞が読めなくなることを防ぐ床である。確定値は最終描画画素のコントラスト比を測る #99・#102 と #33 の
 * 統合で較正するため、ここでは可読性を強く優先する高めの床を初期値に置く。★暫定。
 */
export const READING_ROLE_MIN_OPACITY = 0.7;

/**
 * 同時複製数の上限（縮退の第1対象）。採用理由を先に述べる。複製は1単位の文字数倍の描画を生むため最も費用が
 * 大きく、超過時に最初に減らす対象である。残像トレイル（写し5）や円状重ね（写し8）が同時に数単位出ても
 * 描画命令が破綻しない範囲の初期値とし、実機計測で #97 が確定する。★暫定。
 */
export const EFFECT_MAX_DUPLICATE_COPIES = 48;

/**
 * 発光対象数の上限（縮退の第2対象）。採用理由を先に述べる。発光はブルーム後処理の塗りつぶし負荷を増やすため、
 * 同時に閾値を超える文字数を抑える。サビの最大文字密度でも後処理が破綻しない範囲の初期値とし、#97 が確定する。★暫定。
 */
export const EFFECT_MAX_GLOW_GLYPHS = 64;

/**
 * 1フレーム内の取っ手更新対象数の上限（縮退の第3対象）。採用理由を先に述べる。取っ手の更新は主スレッドの費用で、
 * 1フレーム予算（1000ミリ秒 ÷ 60 = 16.67ミリ秒）の一部に収める必要がある。同時表示文字数に複製の写しを加えた
 * 更新数の上限であり、#97 が実機計測で確定する。★暫定。
 */
export const EFFECT_MAX_UPDATE_TARGETS_PER_FRAME = 320;
