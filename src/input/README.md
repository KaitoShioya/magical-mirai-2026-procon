# input — 入力と当たり判定

- **責務**: Pointer Events の取得、複数の指の独立管理、画面座標の正規化と当たり判定。X軸（画面左側の帯のどのレーンか）を音程スロットへ、効果色をそのレーンから離散的に、純粋な関数で写像する。
- **禁止依存**: 3次元の交差判定（レイキャスト）を使わない。カメラの行列を参照しない。`profiles`・`tools` を import しない。
- **担当Issue**: #47 / #48 / #49
- 詳細は `docs/decisions/architecture.md` を参照する。
